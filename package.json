{
  "name": "aws-keyboard-shortcuts",
  "version": "1.0.0",
  "description": "Adds keyboard shortcuts to the AWS Console",
  "main": "awsKeyboardShortcuts.user.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Mike \"yo1dog\" Moore",
  "license": "ISC",
  "browserify": {
    "transform": [
      "brfs"
    ]
  },
  "devDependencies": {
    "brfs": "^1.4.3"
  }
}
